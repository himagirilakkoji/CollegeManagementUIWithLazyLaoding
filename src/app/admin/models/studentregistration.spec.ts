import { Studentregistration } from './studentregistration';

describe('Studentregistration', () => {
  it('should create an instance', () => {
    expect(new Studentregistration()).toBeTruthy();
  });
});
